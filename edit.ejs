<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Blog</title>
    <link rel="stylesheet" href="edit.css">
</head>

<body>
    <form action="/save" method="POST" class="edit-container">

        <h2>Edit Blog Post</h2>
        <textarea name="content" id="blog-content" rows="10"
            cols="50"><%if(locals.index && locals.blogs){%><%= blogs[index]["content"] %><% }else {%>-><%} %></textarea>
        <br>
        <%if(locals.index){%>
            <button type="submit" name="update" value="<%= index %>" class="save-btn">Update</button>
            <%}else {%>
                <button type="submit" name="save" class="save-btn">Save</button>
                <% } %>

    </form>

    <script src="edit.js"></script>
</body>

</html>